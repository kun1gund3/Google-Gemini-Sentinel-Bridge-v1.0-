# Security Policy
- **Sentinel-Audit:** Automatischer Check der .env-Integrität bei jedem Start.
- **Firewall:** Überwachung der Termux-Netzwerk-Ports via `netstat` (simuliert).
- **Encryption:** Alle Keys werden in `api_key.txt` (chmod 600) isoliert.
